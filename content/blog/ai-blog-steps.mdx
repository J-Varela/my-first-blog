# setting up a project

## Backend

1. Create a supbase project add tables (id, createAt, content, title, imageUrl, userId )

# Nextjs

- add shadcn/ui to the project.
- set supabase url and key in env.local
- add functions to get data from supabase.(lib/supabase.ts)
- Application shell - add the header component
- Home Page
- Form component
- Client action
- Server action
- install openai
- add OPENAI_API_KEY to env.local
- OpenAI models: GPT-4, DALL-E
- Install `base64-arraybuffer` to decode base64 images

- Add [blog] Page
- Add `redirect` to the server action
- add `revalidatePath` to the server action
- Submit button
- Toast component
- Recent blogs section
